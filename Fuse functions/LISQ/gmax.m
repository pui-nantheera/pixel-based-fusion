function g = gmax(A, B)
%------------------------------------------------------------------------------
% UNUSED??
%------------------------------------------------------------------------------
if ~all(size(A) == size(B))
  error(' gmax - dimensions do not match ')
end
D=(A>B);
g= D.*A+(~D).*B;
%------------------------------------------------------------------------------
